{% extends 'app.html.twig' %}

{% block content %}
    <div class="card">
        <div class="card-header d-flex justify-content-center align-items-center">
            <h5 class="card-title mr-auto mb-0">{{ book.name }}</h5>
            <a href="{{ path('book_index') }}" class="btn btn-primary mr-2">{{ 'main.back'|trans }}</a>
             {{ include('book/_delete_form.html.twig', {'className': 'btn-danger'}) }}
        </div>
        <div class="card-body">
            {{ include('book/_form.html.twig', {'button_label': 'main.update'}) }}
        </div>
    </div>
{% endblock %}
