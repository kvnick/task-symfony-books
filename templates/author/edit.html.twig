{% extends 'app.html.twig' %}

{% block content %}
    <div class="card">
        <div class="card-header d-flex justify-content-center align-items-center">
            <h5 class="card-title mr-auto mb-0">{{ author.fullname }}</h5>
            <a href="{{ path('author_index') }}" class="btn btn-primary mr-2">{{ 'main.back'|trans }}</a>
             {{ include('author/_delete_form.html.twig', {'className': 'btn-danger'}) }}
        </div>
        <div class="card-body">
            {{ include('author/_form.html.twig', {'button_label': 'main.update'}) }}
        </div>
    </div>
{% endblock %}

