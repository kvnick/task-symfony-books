{% extends 'app.html.twig' %}

{% block content %}
    <div class="card">
        <div class="card-header d-flex justify-content-center align-items-center">
            <h5 class="card-title mr-auto mb-0">{{ 'authors.add'|trans }}</h5>
            <a href="{{ path('author_index') }}" class="btn btn-primary btn-xs">{{ 'main.back'|trans }}</a>
        </div>
        <div class="card-body">
            {{ include('author/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
